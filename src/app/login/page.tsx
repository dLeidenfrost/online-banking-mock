import { LoginSwitcher } from "./_components/login-switcher";

export default function loginPage() {
  return (
    <section id="login" className="h-full pt-48 relative flex justify-center">
      <LoginSwitcher />
    </section>
  );
}
